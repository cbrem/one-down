function Environment () {
    var units = [
        [
            [],
        ],
        [
            [],
        ],
        [
            [],
        ],
    ];

    this.draw

    //request an environment frame that's 20 blocks by 25 blocks
    //(600 X 750 pixels). Note that this
    this.newFrame = function () {

        var unitNum = Math.floor(math.random()*)
    }
}
